import React from 'react';
import styled from "styled-components";

const FancyHeader = styled.h1`
  text-align: center;
`;

const LoginPage = () => {
  return (
    <FancyHeader>Login page</FancyHeader>
  );
};

export default LoginPage;